# Converge WiFi Manager

See `README.md` for the current implementation status.

This project follows the provided goal: a cross-platform C++23 CLI for managing an owned or authorized Converge ZTE F670L router using clean architecture. Router-specific HTTP behavior is intentionally left behind `IRouterClient` until the ZTE F670L endpoints are documented or captured.
